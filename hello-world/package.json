{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "Hello World to WebAssembly using Bazel and Emscripten",
  "main": "javascript/index.js",
  "scripts": {
    "demo": "RUN_DEMO=wasm node ./javascript/examples/demo.js",
    "build": "npm run build:wasm",
    "build:wasm": "bash scripts/build-wasm.sh && npm run build:copy",
    "build:copy": "bash scripts/build-copy.sh",
    "clean": "bazel clean --expunge && rm -rf javascript/bin/* && rm -rf javascript/dist/*",
    "prerollup": "rm -rf javascript/bin/* && rm -rf javascript/dist/* && npm run build:copy",
    "rollup": "rollup -c"
  },
  "devDependencies": {
    "rollup": "^2.79.1",
    "rollup-plugin-terser": "^7.0.2"
  }
}
